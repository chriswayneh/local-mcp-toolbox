# Explicit Docker-only policy for the opt-in socket-proxy container profile.
profile: standard
integrations:
  docker: true
audit:
  path: /var/lib/local-mcp-toolbox/audit/events.jsonl
  retention_days: 30
