# Deliberately insecure fixture

This directory contains non-runnable review artifacts with risky settings such
as a root user and a privileged container.  They are fixtures for illustrating
that the infrastructure inventory can find configuration files; Version 1 does
not parse, execute, remediate, or recommend deploying their contents.
